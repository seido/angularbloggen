'use strict';

describe('Directive: insertScript', function () {
  beforeEach(module('anblogApp'));

  var element;

  //it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //  element = angular.element('<insert-script></insert-script>');
  //  element = $compile(element)($rootScope);
  //  expect(element.text()).toBe('this is the insertScript directive');
  //}));
});
