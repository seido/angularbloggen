'use strict';

describe('Directive: hatenaPlugin', function () {
  beforeEach(module('anblogApp'));

  var element;

  //it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //  element = angular.element('<hatena-plugin></hatena-plugin>');
  //  element = $compile(element)($rootScope);
  //  expect(element.text()).toBe('this is the hatenaPlugin directive');
  //}));
});
